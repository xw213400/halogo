PROJECT (halogo)

CMAKE_MINIMUM_REQUIRED (VERSION 3.13)

ADD_DEFINITIONS(-std=c++11)

AUX_SOURCE_DIRECTORY(src DIR_SRCS)

INCLUDE_DIRECTORIES (include)

SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin)

ADD_EXECUTABLE (xrecord ${DIR_SRCS})